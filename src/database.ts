// import { Pool } from 'pg';

// const pool = new Pool({
//     user: process.env.DB_USER,
//     database: process.env.DB_DATABASE,
//     password: process.env.DB_PASSWORD,
// });

// pool.connect();

// export default pool;